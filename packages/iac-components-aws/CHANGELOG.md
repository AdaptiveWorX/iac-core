# Changelog

All notable changes to `@adaptiveworx/iac-components-aws` are documented in
this file. Entries are generated by `nx release` from
[Conventional Commits](https://www.conventionalcommits.org).

## 0.7.0 (unreleased)

### Changed

- **BREAKING**: Renamed from `@adaptiveworx/iac-components` to
  `@adaptiveworx/iac-components-aws` as part of the flux-core monorepo
  restructure. AWS components and AWS-IAM naming helpers (formerly
  `src/shared/`) now live in this package. No API changes.

## Pre-rename history

For changes prior to the `iac-components` → `iac-components-aws` rename, see
the git history of `@adaptiveworx/iac-components` 0.6.x.
