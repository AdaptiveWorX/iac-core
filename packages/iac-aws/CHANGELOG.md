# Changelog

All notable changes to `@adaptiveworx/iac-aws` are documented
in this file. Entries are generated by `nx release` from
[Conventional Commits](https://www.conventionalcommits.org).

## 0.1.0 (unreleased)

Initial publish from the `flux-core` monorepo. Contents:

- `SharedVpc` — multi-tier VPC with NAT, flow logs, RAM sharing
- `CrossAccountIAMRoles` — cross-account Pulumi role + foundation access role
- `GitHubActionsOIDC` — OIDC provider + GitHub Actions deploy role
- IAM policy helpers
- AWS-IAM naming helpers (formerly `src/shared/`)

This package supersedes the never-published `@adaptiveworx/iac-components`
working name; nothing was ever shipped under that name.
